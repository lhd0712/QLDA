Flask
=====
Flask là một micro web framework cho Python.

Cách chạy ứng dụng:
1. Cài đặt Flask:
   pip install flask
2. Chạy ứng dụng:
   python app.py

Cấu trúc thư mục:
- app.py: file chính chạy Flask
- templates/: chứa file HTML
- static/: chứa file tĩnh (CSS, JS, ảnh)